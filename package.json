{
  "name": "claude-usage",
  "version": "1.0.0",
  "description": "OpenClaw skill for monitoring Claude API usage and costs",
  "main": "dist/index.js",
  "bin": {
    "claude-usage": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test dist/tests/*.test.js",
    "prebuild": "rm -rf dist",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "type": "module",
  "engines": {
    "node": ">=18"
  }
}
